# 🔌 Зависимость от базы данных

## ❓ Может ли сервер работать без базы данных?

**ДА!** Серверы могут запуститься и работать **БЕЗ базы данных**, но с ограниченной функциональностью.

## 📊 Как это работает

### ✅ Сервер запускается БЕЗ БД

Серверы используют **ленивую инициализацию** базы данных:
- Сервер запускается успешно
- БД подключается только при первом запросе, который требует данных
- Если БД недоступна, сервер продолжает работать

### ⚠️ Что происходит без БД

1. **Сервер запускается** ✅
2. **Health check работает** ✅ (показывает статус БД)
3. **API эндпоинты возвращают ошибку 503** ⚠️ (Database unavailable)
4. **Сервер не падает** ✅

### 🔍 Проверка статуса БД

Все серверы имеют health check, который показывает статус БД:

```bash
GET /health
```

Ответ:
```json
{
  "status": "ok",
  "checks": {
    "database": {
      "status": "degraded",
      "message": "Database not available - server can work in degraded mode",
      "canWorkWithoutDB": true
    }
  }
}
```

## 🛠️ Технические детали

### Подключение к БД

Серверы подключаются к БД через **прямой импорт модулей** (не через HTTP):
- Путь: `DatabaseServer/src/index.js`
- Ленивая инициализация при первом запросе
- Graceful degradation при недоступности

### Обработка ошибок

Если БД недоступна:
- API возвращает статус `503 Service Unavailable`
- Сообщение: `"Database is not available"`
- Код ошибки: `DATABASE_UNAVAILABLE`
- Сервер продолжает работать

### Пример ответа без БД

```json
{
  "success": false,
  "error": {
    "message": "Database is not available",
    "code": "DATABASE_UNAVAILABLE",
    "canWorkWithoutDB": true
  }
}
```

## 📝 Какие серверы зависят от БД

### Требуют БД:
- ✅ **api.mifistix.com** - все эндпоинты требуют БД
- ✅ **id.mifistix.com** - регистрация и авторизация требуют БД

### НЕ требуют БД:
- ✅ **promo.mifistix.com** - может работать без БД
- ✅ **blog.mifistix.com** - может работать без БД
- ✅ **support.mifistix.com** - может работать без БД
- ✅ **test.mifistix.com** - может работать без БД
- ✅ **staging.mifistix.com** - может работать без БД
- ✅ **dev.mifistix.com** - может работать без БД
- ✅ **cron.mifistix.com** - может работать без БД
- ✅ **backup.mifistix.com** - может работать без БД
- ✅ **analytics.mifistix.com** - может работать без БД
- ✅ **mail.mifistix.com** - может работать без БД

## 🚀 Рекомендации

### Для разработки:
- Можно запускать серверы без БД для тестирования
- Health check покажет статус БД

### Для production:
- Всегда запускайте DatabaseServer перед другими серверами
- Используйте `start-all.bat` для правильного порядка запуска
- Мониторьте health checks для отслеживания статуса БД

## 🔧 Проверка доступности БД

```bash
# Проверить статус БД через health check
curl http://localhost:3001/health

# Расширенный health check
curl http://localhost:3001/health?detailed=true
```

## 📌 Итог

**Серверы МОГУТ работать без БД**, но:
- ✅ Сервер запускается
- ✅ Health check работает
- ⚠️ API эндпоинты возвращают ошибку 503
- ✅ Сервер не падает

Это позволяет:
- Тестировать серверы независимо
- Разрабатывать без БД
- Graceful degradation при проблемах с БД

