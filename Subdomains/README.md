# 🌐 Поддомены Mifistix

Эта папка содержит серверы для различных поддоменов проекта.

## 📁 Структура

```
Subdomains/
├── api/              # api.mifistix.com - API сервер (порт 3001)
├── id/               # id.mifistix.com - Identity сервер (порт 3002)
├── promo/            # promo.mifistix.com - Promo & Marketing (порт 3003)
├── blog/              # blog.mifistix.com - Blog & Content (порт 3004)
├── support/           # support.mifistix.com - Support & Help Desk (порт 3005)
├── test/              # test.mifistix.com - Testing & QA (порт 3006)
├── staging/           # staging.mifistix.com - Staging Environment (порт 3007)
├── dev/               # dev.mifistix.com - Development Environment (порт 3008)
├── cron/              # cron.mifistix.com - Cron Jobs & Scheduled Tasks (порт 3009)
├── backup/            # backup.mifistix.com - Backup & Recovery (порт 3010)
├── analytics/         # analytics.mifistix.com - Analytics & Metrics (порт 3011)
└── mail/              # mail.mifistix.com - Mail & Email Server (порт 3012)
```

## 🚀 Поддомены

### api.mifistix.com (Порт: 3001)
API сервер для работы с данными пользователей, постов и медиа.

**Эндпоинты:**
- `GET /api/users/:uid` - получить профиль пользователя
- `GET /api/users/:uid/posts` - получить посты пользователя
- `GET /api/posts/:postId` - получить пост
- `GET /api/posts/:postId/stats` - получить статистику поста
- `GET /health` - проверка здоровья сервера

### id.mifistix.com (Порт: 3002)
Identity сервер для авторизации и регистрации пользователей.

**Эндпоинты:**
- `POST /api/auth/register` - регистрация нового пользователя
- `POST /api/auth/login` - авторизация пользователя
- `GET /api/auth/verify` - проверка валидности токена
- `GET /health` - проверка здоровья сервера

### promo.mifistix.com (Порт: 3003)
Promo & Marketing сервер для промо-кампаний и специальных предложений.

**Эндпоинты:**
- `GET /api/campaigns` - получить список кампаний
- `GET /api/offers` - получить специальные предложения
- `GET /health` - проверка здоровья сервера

### blog.mifistix.com (Порт: 3004)
Blog & Content сервер для блога и контента.

**Эндпоинты:**
- `GET /api/posts` - получить список постов блога
- `GET /api/posts/:id` - получить пост блога
- `GET /api/categories` - получить категории блога
- `GET /health` - проверка здоровья сервера

### support.mifistix.com (Порт: 3005)
Support & Help Desk сервер для поддержки пользователей.

**Эндпоинты:**
- `GET /api/tickets` - получить список тикетов
- `POST /api/tickets` - создать тикет
- `GET /api/faq` - получить FAQ
- `GET /api/knowledge` - получить базу знаний
- `GET /health` - проверка здоровья сервера

### test.mifistix.com (Порт: 3006)
Testing & QA сервер для тестирования.

**Эндпоинты:**
- `GET /api/test` - тестовый эндпоинт
- `POST /api/mock` - mock эндпоинт для тестирования
- `GET /health` - проверка здоровья сервера

### staging.mifistix.com (Порт: 3007)
Staging Environment сервер для предпродакшн тестирования.

**Эндпоинты:**
- `GET /api` - API эндпоинт
- `GET /health` - проверка здоровья сервера

### dev.mifistix.com (Порт: 3008)
Development Environment сервер для разработки.

**Эндпоинты:**
- `GET /api` - API эндпоинт
- `GET /api/debug` - отладочная информация
- `GET /health` - проверка здоровья сервера

### cron.mifistix.com (Порт: 3009)
Cron Jobs & Scheduled Tasks сервер для выполнения задач по расписанию.

**Эндпоинты:**
- `GET /api/jobs` - получить список задач
- `GET /api/status` - получить статус задач
- `GET /health` - проверка здоровья сервера

### backup.mifistix.com (Порт: 3010)
Backup & Recovery сервер для резервного копирования.

**Эндпоинты:**
- `GET /api/status` - получить статус бэкапов
- `GET /api/backups` - получить список бэкапов
- `POST /api/backups/create` - создать бэкап
- `GET /health` - проверка здоровья сервера

### analytics.mifistix.com (Порт: 3011)
Analytics & Metrics сервер для аналитики и метрик.

**Эндпоинты:**
- `GET /api/stats` - получить статистику
- `GET /api/metrics` - получить метрики
- `POST /api/events` - отправить событие
- `GET /health` - проверка здоровья сервера

### mail.mifistix.com (Порт: 3012)
Mail & Email сервер для отправки писем.

**Эндпоинты:**
- `POST /api/send` - отправить письмо
- `GET /api/status` - получить статус очереди
- `GET /api/templates` - получить шаблоны писем
- `GET /health` - проверка здоровья сервера

## 🔧 Установка

Для каждого поддомена необходимо установить зависимости:

```bash
# Используйте install-all.bat в корне проекта для установки всех зависимостей
install-all.bat
```

Или установите зависимости для конкретного поддомена:

```bash
cd Subdomains/[subdomain-name]
npm install
```

## ⚙️ Настройка

Все поддомены используют централизованную конфигурацию из `shared/config/index.js`.

Переменные окружения можно настроить через `.env` файлы в корне проекта или в каждом поддомене.

## 🎯 Запуск

### Запуск всех поддоменов:

Используйте `start-all.bat` в корневой папке проекта:

```bash
start-all.bat
```

### Запуск отдельного поддомена:

```bash
cd Subdomains/[subdomain-name]
npm start
```

## 🔗 Интеграция

Все поддомены используют:
- Общую конфигурацию из `shared/config`
- Общие утилиты из `shared/utils`
- Общие middleware из `shared/middleware`
- Общий DatabaseServer для работы с данными

## 📝 Примечания

- Каждый поддомен работает как отдельный независимый сервер
- Все поддомены используют общий `DatabaseServer` для хранения данных
- CORS настроен для работы с соответствующими поддоменами
- Rate limiting настроен для защиты от злоупотреблений (кроме test и dev)
- Health checks доступны на всех поддоменах по пути `/health`
