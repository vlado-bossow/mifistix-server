# ☁️ Cloudflare Tunnel для локальной работы

## 🚀 Быстрый старт

### 1. Настройка DNS записей на localhost:

```cmd
cd cloudflare
setup-dns-localhost.bat
```

Этот скрипт:
- ✅ Установит cloudflared (если нужно)
- ✅ Авторизуется в Cloudflare через браузер
- ✅ Создаст туннель `mifistix-local`
- ✅ **Настроит все DNS записи через `cloudflared tunnel route dns`**
- ✅ Создаст config.yml

### 2. Проверка DNS записей:

```cmd
cd cloudflare
check-dns.bat
```

### 3. Запуск всех серверов + туннель:

```cmd
cd cloudflare
start-all-with-tunnel.bat
```

Или отдельно:
```cmd
# В корне проекта
start-all.bat

# В папке cloudflare (в другом окне)
start-tunnel.bat
```

## 📁 Файлы

- `setup-dns-localhost.bat` - **Главный скрипт** - Настройка DNS записей на localhost через Tunnel
- `setup-dns-api.bat` - Альтернативный способ через Cloudflare API (требует Zone ID)
- `check-dns.bat` - Проверка существующих DNS записей
- `start-tunnel.bat` - Запуск Cloudflare Tunnel
- `start-all-with-tunnel.bat` - Запуск всех серверов + туннель
- `config.yml` - Конфигурация туннеля (создаётся автоматически)

## 🔧 Как это работает

1. **Cloudflare Tunnel** создаёт безопасное соединение между Cloudflare и вашим localhost
2. **DNS записи** настраиваются через `cloudflared tunnel route dns` - автоматически создаются CNAME записи
3. Туннель маршрутизирует запросы на localhost:порт согласно config.yml
4. Всё работает локально, без внешнего IP!

## ⚠️ Важно

- **Туннель должен быть запущен** пока работают серверы
- Все серверы должны быть запущены на localhost
- Порт 80/443 не нужен - всё идёт через Cloudflare
- DNS записи создаются автоматически через `cloudflared tunnel route dns`

## 🐛 Решение проблем

### DNS записи не создались?

1. Проверьте авторизацию: `cloudflared tunnel list`
2. Проверьте туннель: `cloudflared tunnel info mifistix-local`
3. Запустите `check-dns.bat` для проверки записей

### Туннель не подключается?

1. Убедитесь, что все серверы запущены
2. Проверьте config.yml - правильные ли порты
3. Проверьте логи туннеля в окне запуска
